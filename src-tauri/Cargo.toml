[package]
name = "crypto-strategy-assistant"
version = "0.1.0"
description = "A local, deterministic crypto strategy decision-support app"
authors = ["Crypto Strategy Assistant contributors"]
edition = "2021"

[lib]
name = "crypto_strategy_assistant_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

